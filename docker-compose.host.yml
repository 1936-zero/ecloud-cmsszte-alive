# DEPRECATED: host network is now the default in docker-compose.yml.
# This file is kept so old docs/scripts still merge cleanly (no-op / reinforces host).
#
# Prefer:
#   docker compose up -d --build
#
# For Windows / macOS Docker Desktop use bridge instead:
#   docker compose -f docker-compose.yml -f docker-compose.bridge.yml up -d --build
services:
  ecloud-keepalive:
    network_mode: host
