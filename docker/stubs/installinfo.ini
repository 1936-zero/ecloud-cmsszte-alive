; Stub installinfo for CI / HTTP-only WebUI (NO Path B mint).
; Real mint needs host CMSS client [PublicKey] csap_id (16-char AES key).
; docker-compose default mounts the host official path; only set
;   INSTALLINFO_HOST=./docker/stubs/installinfo.ini
; when you intentionally skip SPICE credential mint.
;
; Section name MUST be PublicKey (not [install]) — see l3/aes_decode_connstr.py
[PublicKey]
csap_id=
